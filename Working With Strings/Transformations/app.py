# Transformation
# Store the first name in a variable.
first_name = "Michael"

# Store the last name in another variable.
last_name = "Jordan"

# Combine the two strings with a hyphen in between.
# This creates a new full name value and reassigns it to last_name.
last_name = first_name + "-" + last_name

# Print the transformed full name.
print(last_name)

# Save the folder path as a string.
folder = "C:/Users/Barraaa/"

# Save the file name as a string.
file = "report.csv"

# Join the folder and file name to create the full file path.
full_file_path = folder + file

# Print the final file path.
print(full_file_path)